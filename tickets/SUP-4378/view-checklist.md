# SUP-4378 — view rollout checklist

6 new metrics (FP ST%, updated ST%, WOH, FP WOH, WOF, FP Weeks, FP Wkly ST%) across
~24 views per the ticket's "Current Setup - Future Expectations/Details" section.
Metrics 1-4 (FP ST%, updated ST%, WOH, FP WOH) are formula-complete; 5-7 (WOF, FP
Weeks, FP Wkly ST%) are pending the `dbt_wk` vs `act_dbt_wk` data check + Nicole's
RP Weeks clarification (see conversation) — also pending Jatin's call on
first-sale-week/latest-MD-week approach (attribute join vs. lifecycle scan).

**2026-08-20 ticket update**: Nicole re-edited the description. Several view
groups are now tagged **(ST metrics only)** — they need just FP ST% + the updated
ST% fix, not the full 6-metric set. This shrinks WOF/FP Weeks/FP Wkly ST%'s
footprint to roughly half the views below (marked ⭐ below). Everything else
(formulas, AC, wrench order) is unchanged from the original read.

File paths are relative to `belk-configs/`. "Status" tracks the **wrench/pivot
wiring**, not just file identification — a row only flips to done once the pivot
carries the metric AND the wrench exposes it. "QA Status" (where the table has
the column) tracks whether the human has manually deployed and eyeballed the
change in the live BELK QA environment — separate from "Status", which only
means the code is written and internally consistent.

## Hindsighting — Top Down / Bottom Up

### Style Color Review

| Tab | Files | Pivot | Status | QA Status |
|---|---|---|---|---|
| Summary View (all tabs) | `uidefn/view/HistoryStyleColorSummaryWorklist.viewdefn`, `HistoryStyleColorCardViewDetailsSummary_quick.viewdefn`, `HistoryStyleColorCardViewDetailsSummary.viewdefn`, `HistoryStyleColorReviewRollUp.viewdefn` | `HistoryFit_quick` / `HistoryFit_detail` | **DONE** (metrics 1-4, this session) | **Deployed & verified in QA** (bottom-up) — 2026-08-23. **`tot_weekcount` fix 2026-08-25** (bug found via manual unit-testing doc + confirmed by Nicole/client): `WOH`/`FP WOH` were dividing by the existing `weekcount`, which is prodlife-filtered (`countIf(DISTINCT time, strcntwk>0 AND prodlife IN (${LEAF_PRODLIFE}))`) — so both metrics shifted when the UI's Price Status filter was applied, even though `tot_avail_inv_u`/`tot_net_sls_u`/`fp_net_sls_u` correctly stayed agnostic. Root-caused precisely via live BELK query (stylecolor 355413234.007: weekcount 4→3 under FP filter, reproducing WOH 28.9→21.7 and FP WOH 134.9→101.2 exactly). Added `tot_weekcount_x` (same `countIf`, minus the prodlife condition) to the shared `pivot/include/history_metrics_ty.ftl`'s 3 base tables (SALES real value, RCPT/OO zero-filled, matching existing pattern); threaded `tot_weekcount` through both pivots' `BASE_METRICS_UNION_PRUNED`/`BASE_METRICS_AGG_UNION_PRUNED` layers; swapped `weekcount`→`tot_weekcount` in the `woh`/`fp_woh` formulas only (left `weekcount` untouched everywhere else — `avg_wk_inv_u` etc. legitimately stay filter-sensitive). Noted (not fixed, out of scope, harmless): a pre-existing column-order mismatch between SALES and RCPT/OO further up the same stretch, but every value in that range is uniformly zero so it has no functional effect. | **Deployed & verified in QA** — 2026-08-25 (Quick + Detail tabs, both confirmed via updated screenshots: WOH/FP WOH now stay identical — 28.9/134.9 — whether the Price Status filter is applied or not). **Top-Down 2026-08-25**: `TD_HistoryFit_quick.pivotdefn`/`TD_HistoryFit_detail.pivotdefn` were sitting at zero SUP-4378 content (confirmed via `td_summary_quick.txt` runtime log — same `history_metrics_ty.ftl` architecture as bottom-up, raw agnostic columns already available from the shared include, but the TD pivots' own `BASE_METRICS_UNION_PRUNED`/`AGG_UNION_PRUNED`/`COMPUTED_METRICS_PRUNED` layers were completely untouched). Ported the bottom-up file's full current `COMPUTED_METRICS_PRUNED`/`BASE_METRICS_UNION_PRUNED`/`BASE_METRICS_AGG_UNION_PRUNED` content into both TD files in one pass (both original SUP-4378 metrics + `tot_weekcount` fix together, since nothing existed yet) — verified byte-identical to bottom-up's corresponding blocks (including matching the exact split position: the big `tot_shp_u`/`fp_net_sls_u`/`woh`/`fp_woh` block sits right after `avg_wk_inv_u`, separate from the `sell_thru_pct` fix which sits later after `avg_inv_u_per_cc`, matching bottom-up precisely — caught and corrected a first-pass mistake where both were bundled in one spot). No viewdefn changes — no TD_ wrench variants exist, they fall back to the already-fixed bottom-up viewdefns. Covers Summary View + Grid View top-down (shared pivots). Verified: 37 triple-quotes balanced, 0 adjacency issues in both files. | **Top-Down: DONE — awaiting QA validation** |
| Grid View | `HistoryStyleColorGridViewGrid.viewdefn`, `HistoryStyleColorGridViewGrid_quick.viewdefn`, `HistoryStyleColorGridWorklist.viewdefn` | `HistoryFit_quick` / `HistoryFit_detail` — same pivots as Summary, no pivot changes needed | **DONE** (FP ST%, updated ST%, WOH, FP WOH — wrench only, no rollup changes since `HistoryStyleColorReviewRollUp.viewdefn` is shared and already correct) | **Deployed & verified in QA** (bottom-up) — 2026-08-23. **`tot_weekcount` fix 2026-08-25**: same pivot-layer fix as Summary View (shared pivots) plus updated the client-side rollup `"formula"` fields in both `HistoryStyleColorGridViewGrid_quick.viewdefn` and `HistoryStyleColorGridViewGrid.viewdefn` — `avg(weekcount)` → `avg(tot_weekcount)` for both `fp_woh` and `woh` (4 formula edits total, 2 per file). `HistoryStyleColorReviewRollUp.viewdefn` confirmed to have no `woh`/`fp_woh` entries, nothing to change there. | **Deployed & verified in QA** — 2026-08-25 (Quick + Detail tabs). **Top-Down 2026-08-25**: covered by Summary View's `TD_HistoryFit_quick`/`_detail.pivotdefn` port (same shared pivots) — no separate pivot work; wrench falls back to the already-fixed bottom-up viewdefns (no TD_ variant exists). | **Top-Down: DONE — awaiting QA validation** |
| Flow Type | `HistoryStyleColorCardViewDetailsFlowType.viewdefn` (`HistoryStyleColorFlowTypeDetails.viewdefn`/`HistoryStyleFlowTypeDetails.viewdefn` are unused here — those belong to a separate style-level `HistorySummary` review, confirmed via `AssortmentUiConf.confdefn`) | **Bottom-up**: `HistoryFit` — different pivot from Summary/Grid, needed its own `COMPUTED_METRICS_PRUNED` edit (now done). **Top-down**: `TD_HistoryFit` — HOLD, needs separate investigation. (Correction 2026-08-25: the "`TD_` prefix ⇒ `reverseAggSQLs`" theory this row implied does not hold — checked against darwin source, `TD_*.pivotdefn` files mostly use `aggregationSQLs` like everything else; no naming-based branch exists in the Java code. See `knowledge/backend/darwin-primer.md`.) | **Bottom-up DONE**; Top-down not started. **`tot_weekcount` fix 2026-08-25**: `HistoryFit.pivotdefn` shares `history_metrics_ty.ftl` (already has `tot_weekcount_x`, added earlier for Summary/Grid View), so only needed its own pass-through in `BASE_METRICS_UNION_PRUNED`/`BASE_METRICS_AGG_UNION_PRUNED` + `weekcount`→`tot_weekcount` swap in the `woh`/`fp_woh` formulas — grouped at the end of the SUP-4378 section (no separate comment, matching the earlier convention fix). `HistoryStyleColorCardViewDetailsFlowType.viewdefn` confirmed to have no `formula` override for `woh`/`fp_woh` (direct dataIndex binding, same as Summary View's card wrench) — no viewdefn change needed. Verified: 37 triple-quotes balanced, 0 comment/params.X adjacency, UNION_PRUNED/AGG_UNION_PRUNED both 66 cols. | **Deployed & verified in QA** (bottom-up) — 2026-08-23, incl. `tot_weekcount` fix — 2026-08-25. |
| Top TY vs LY | `HistoryStyleColorTopTYvsLYDetails.viewdefn`, `HistoryStyleColorCardViewDetailsTopTYLY.viewdefn` | verify | Not started | — |
| Top TY vs LY | `HistoryStyleColorCardViewDetailsTopTYLY.viewdefn`, `HistoryStyleColorReviewTYLYRollUp.viewdefn` | `HistoryFitTYLY` — different pivot AND different include file (`history_metrics_ty_ly.ftl`, not `history_metrics_ty.ftl`). Has 6 base tables (TY+LY × sales/rcpt/oo); TY and LY share the same `COMPUTED_METRICS_PRUNED`/`BASE_METRICS_UNION_PRUNED` params (one edit covers both), output as separate rows tagged by a `key` field (`'TY'`/`'LY'`), not separate ly_-prefixed columns. | **DONE**. **`tot_weekcount` fix 2026-08-25**: `history_metrics_ty_ly.ftl` didn't have `tot_weekcount_x` yet (separate include from `history_metrics_ty.ftl`) — added it to all 6 base tables (TY-SALES/LY-SALES real value, TY-RCPT/LY-RCPT/TY-OO/LY-OO zero-filled), grouped at the end of each existing SUP-4378 section; verified positional parity within each 3-table union (TY's own SALES/RCPT/OO, LY's own) — `tot_weekcount_x` lands at position 106 in all 6, matching the earlier convention. `HistoryFitTYLY.pivotdefn` needed only ONE pass-through + formula edit each (shared TY/LY params, per this row's own note above). `HistoryStyleColorCardViewDetailsTopTYLY.viewdefn`/`HistoryStyleColorReviewTYLYRollUp.viewdefn` confirmed no formula overrides — no viewdefn changes needed. Verified: 33 triple-quotes balanced, 0 adjacency issues, UNION_PRUNED/AGG_UNION_PRUNED both 67 cols. | **Deployed & verified in QA** (bottom-up) — 2026-08-23, incl. `tot_weekcount` fix — 2026-08-25. |
| TY/LY Grid View | `HistoryStyleColorTYLYGrid_quick.viewdefn` (Quick), `HistoryStyleColorTYLYGrid_detail.viewdefn` (Detail — corrected), `HistoryStyleColorTYLYGridRollup.viewdefn` | `HistoryFitTYLYTranspose_quick`/`_detail` — a THIRD distinct pivot, genuinely transposes TY/LY into one wide row (`ty_*`/`ly_*` prefixed columns via rename+zero-fill+reagg, not row-tagging like `HistoryFitTYLY`). New metrics needed `tot_avail_inv_u`/`tot_net_sls_u`/`fp_net_sls_u` exposed as `ty_`/`ly_`-prefixed columns too (6 touch points each, both files) so grid-group formulas can recompute from raw components like every other metric in this file does. | **DONE** — **correction 2026-08-23**: initially edited the wrong Detail-tab wrench file. Confirmed via `ty_ly_grid_detail.txt` runtime log that the Detail tab's `"view"` array (in `HistoryStyleColorTYLYGridWorklist.viewdefn`) points to `HistoryStyleColorTYLYGrid_detail.viewdefn`, not the bare `HistoryStyleColorTYLYGrid.viewdefn` — the bare file is actually wired to `TD_HistoryStyleColorTYLYGridWorklist.viewdefn` (top-down, still on hold), confirmed by grep. Bare file's edits left in place (harmless — TD pivot isn't built yet, so those fields just render blank until that work happens) but the real bottom-up Detail wrench (`_detail.viewdefn`) had zero of the new-metric changes and its pre-existing `ty_sell_thru_pct`/`ly_sell_thru_pct` formulas were still on the old buggy denominator (`avail_inv_u` not `tot_avail_inv_u`) — both now fixed to match Quick's pattern exactly (FP ST%/FP WOH/WOH added, ST% formula corrected). **`tot_weekcount` fix 2026-08-25**: `history_metrics_ty_ly.ftl` fixed first (same as Top TY vs LY — 6 base tables, `tot_weekcount_x` added to all). `HistoryFitTYLYTranspose_quick`/`_detail` needed the fullest treatment of any file so far — mirrored the EXACT 6-touch-point pattern already established for `tot_avail_inv_u`/`tot_net_sls_u`/`fp_net_sls_u`: Layer 1 pass-through (`BASE_METRICS_UNION_PRUNED`/`AGG_UNION_PRUNED`) + `woh`/`fp_woh` formula swap, PLUS Layer 2 transpose (`ty_tot_weekcount`/`ly_tot_weekcount` — 2 in `FINAL_COLUMN_LIST`'s aggregation section, 4 in the real/zero-fill transpose blocks) — 10 touch points total per file. Relevant to **Open Item 6** (`avg` vs `max` weekcount aggregation for grouped rows, still held pending DB verification): the wrench's own client-side `"formula"` fields do a group-level recompute using `avg(ty_weekcount)`/`avg(ly_weekcount)` — swapped to `avg(ty_tot_weekcount)`/`avg(ly_tot_weekcount)` (same fix as everywhere else — which weekcount, not which aggregation function). Did **not** touch the `avg`→`max` question itself, per the existing hold. `HistoryStyleColorTYLYGridRollup.viewdefn` confirmed to have no `woh`/`weekcount` references. Verified: both pivotdefns 43 triple-quotes balanced, 0 adjacency issues, 10/10 `tot_weekcount` touch points each; both viewdefns valid JSON, 4/4 formula swaps each, zero stragglers. | **Deployed & verified in QA** (bottom-up) — 2026-08-23, incl. `tot_weekcount` fix — 2026-08-25. |

### Category Recap ⭐ (ST metrics only per 2026-08-20 update — FP ST% + updated ST%, skip WOH/FP WOH/WOF/FP Weeks/FP Wkly ST%)

| Tab | Files | Pivot | Status |
|---|---|---|---|
| Cat Summary — Quick | `HistoryCatRecapCatSummaryGrid_quick.viewdefn` | `HistoryAggregateViewsNestedAttribute_for_CatRecap_quick` — bespoke pivot family (`mother_of_All_Pivots`), LEVEL_1×LEVEL_4 × TY/LY, no shared include file | **DONE — reworked 2026-08-23**: original single-pass reinjection (WHERE-clause prodlife removed, reinjected via `sumIf(x, prodlife IN (${LEAF_PRODLIFE}))` inside `params.BASE_SUM_SALES_METRICS`) broke in QA — `${LEAF_PRODLIFE}` reached ClickHouse unresolved/literal, confirmed via `catrecap_error1.txt` + darwin source ([PivotTemplatizerImpl.java](file:///Users/vigneshn/Desktop/AI%20World/Lineage%20works/darwin/assortment/src/main/java/com/darwin/service/pivot/PivotTemplatizerImpl.java)): a scope variable interpolated *inside* a `params.X="""..."""` string only resolves correctly when that block lives in an `<#include>`'d `.ftl` file — bespoke pivots with no include (this whole family) don't get that second interpolation pass. Reworked to the two-pass **Approach A** backup ([approach-a-narrow-agnostic-table-BACKUP.sql](../../tickets/SUP-4378/approach-a-narrow-agnostic-table-BACKUP.sql)): reverted the WHERE-clause removal and the `sumIf` reinjection back to original (existing metrics untouched, still filtered via the WHERE clause — which works fine as main-body text), and added a new, separate, unfiltered `TY/LY_BASE_SUM_SALES_METRICS_AGNOSTIC` table (6th branch in the `TY_ALL_BASE`/`LY_ALL_BASE` UNION ALL) computing only the 10 new `tot_`/`fp_` columns — no `${LEAF_PRODLIFE}` interpolation inside any params.X block anywhere now (verified 0 occurrences). Downstream (`COMPUTED_METRICS`, `TY_COMBO_ALL`/`LY_COMBO_ALL`, `FINAL_ALL_WITHOUT_VAR`) untouched — pass-through logic is agnostic to source table. **Deployed & verified in QA** — 2026-08-23. CatRecap Detail / Aggregate Views Quick+Detail (same family, same bug class) all reworked and validated too — see their own rows. |
| Cat Summary — Detail | `HistoryCatRecapCatSummaryGrid.viewdefn` | `HistoryAggregateViewsNestedAttribute_for_CatRecap` — same `mother_of_All_Pivots` family but a genuine **3-year** (TY/LY/LLY) architecture, not 2-year like Quick; `LEVEL_1_FINAL`/`LEVEL_4_FINAL` UNION ALL three combo blocks (`TY_COMBO_ALL`/`LY_COMBO_ALL`/`LLY_COMBO_ALL`), each needing 2 zero-fill counterparts instead of 1. Base-table/`COMPUTED_METRICS` layer stayed cheap (shared params, substituted 6× instead of Quick's 4×); real cost was the 3rd combo block. Gave `fp_st_pct` full real 3-year treatment (not just zero-filled LLY) since the wrench already has a live ACT/LY/LLY 3-column "ST %" group and the base layer computes it for free. **DONE — reworked 2026-08-24** to the same two-pass approach as CatRecap Quick (same HOCON/scope-var bug class, same fix): reverted WHERE-clause removal + `sumIf` reinjection on `BASE_SUM_SALES_METRICS`/`BASE_LAST_INV_METRICS` back to original; added 3 new unfiltered `TY/LY/LLY_BASE_SUM_SALES_METRICS_AGNOSTIC` tables (7th UNION ALL branch each, since this file's base-table union already has 6 members incl. `BASE_FIRST_INV_METRICS`, one more than Quick's 5) computing only the new `tot_`/`fp_` columns; `tot_last_value_eoh_u_x`/`_intransit_u_x` (needs a `${TENANT_ID}`/`${SESSION_ID}` subquery) kept out of the params.X block and added inline in each `CREATE TABLE ..._AGNOSTIC_` statement instead. Verified: all 6 original base tables + 3 new AGNOSTIC tables all 98 columns each; zero scope-var refs inside any params.X block (except 2 pre-existing, unrelated `ARR_HIST_*` ones); zero comment-before-`params.X` adjacency. | **Deployed & verified in QA** — 2026-08-24 |
| Cat Summary — Key Financials / Choice Productivity | `HistoryCatRecapCatSummaryKeyFinancials.viewdefn`, `HistoryCatRecapCatSummaryChoiceProductivity.viewdefn` (shared, no `_quick`/detail split) | n/a — pure passthrough, no local formula | **No action needed** (picks up corrected `sell_thru_pct` automatically); FP ST% deliberately not added here per explicit scope instruction |
| Cat Summary — Worklist | `HistoryCatRecapCatSummaryWorklist.viewdefn` | n/a | Not yet checked |
| Cat Summary — Top Down | `TD_HistoryCatRecapCatSummaryGrid.viewdefn` | `TD_...` pivot family — which of `aggregationSQLs`/`reverseAggSQLs` this specific pivot uses needs checking directly (see correction on the Flow Type row above; don't assume by `TD_` prefix) | **HOLD** — not started, same top-down hold as Flow Type/TY-LY Grid |

### Aggregate Views ⭐ (ST metrics only per 2026-08-20 update)

| Tab | Files | Pivot | Status |
|---|---|---|---|
| Nested attribute — Quick | `HistoryAggregateViewsNestedAttributeGrid_quick.viewdefn` | `HistoryAggregateViewsNestedAttribute_quick` — same `mother_of_All_Pivots` family/param shape as Category Recap Quick (confirmed via clean runtime log, `nested_attributes.txt`), but **prodlife collapses at a different layer**: survives as a GROUP BY column through `BASE_SUM_SALES_METRICS`→`ALL_BASE`, only collapses at `BASE_METRICS_UNION`. 4-level hierarchy (`LEVEL_1`-`LEVEL_4`) vs CatRecap's 2-level, but free via shared-param substitution. **DONE — reworked 2026-08-24** to the two-pass approach (same HOCON/scope-var bug class as CatRecap, confirmed before QA testing this time): reverted the WHERE-clause removal on `BASE_SUM_SALES_METRICS`/`BASE_LAST_INV_METRICS` back to original (necessary here, unlike CatRecap, because prodlife must survive past these tables to reach `BASE_METRICS_UNION`); reverted `BASE_METRICS_UNION`'s ~64 real columns from `sumIf(x, prodlife IN (${LEAF_PRODLIFE}))` back to bare `sum()` (its tot_/fp_ pass-through lines were already scope-var-free, left untouched); added a new `TY/LY_BASE_SUM_SALES_METRICS_AGNOSTIC` table (6th UNION ALL branch in `TY_ALL_BASE`/`LY_ALL_BASE`, matching the existing 5 tables' full header shape incl. `location`/`cluster`/`prodlife`) computing only the 10 new columns unfiltered; `tot_last_value_eoh_u_x`/`_intransit_u_x` kept out of the params.X block, added inline in each `CREATE TABLE ..._AGNOSTIC_` statement. Consolidated to a single shared `BASE_SUM_SALES_METRICS_AGNOSTIC` param (TY/LY reference the same block — no per-year duplicate, per the CatRecap consolidation). Verified: all 6 base tables 75 columns each (73 params + 2 inline for AGNOSTIC); zero scope-var refs inside any params.X block; zero comment-before-`params.X` adjacency; `COMPUTED_METRICS`/`TY_COMBO_ALL`/`LY_COMBO_ALL`/`FINAL_ALL_WITHOUT_VAR` confirmed untouched. | **Deployed & verified in QA** — 2026-08-24 |
| Nested attribute — Detail | `HistoryAggregateViewsNestedAttributeGrid.viewdefn` | `HistoryAggregateViewsNestedAttribute_detail.pivotdefn` — log-confirmed (`aggregate_nested_detail.txt`), **not** the bare file guessed earlier. Hybrid architecture: 4-level hierarchy (`LEVEL_1`-`LEVEL_4`, like Quick) × genuine 3-year TY/LY/LLY with `funded_*`/`BASE_FIRST_INV_METRICS` extras (like CatRecap Detail) — `COMPUTED_METRICS` substituted 12× (4 levels × 3 years). Carried over the Quick-specific nuance, not CatRecap Detail's: prodlife survives as a `GROUP BY` column through `BASE_SUM_SALES_METRICS`→`TY_ALL_BASE` (6-table union incl. `BASE_FIRST_INV_METRICS`) and only collapses at `BASE_METRICS_UNION` (~88 columns converted `sum()`→`sumIf(..., prodlife IN (${LEAF_PRODLIFE}))`), not at `BASE_SUM_SALES_METRICS` itself. Wrench already had a live ACT/LY/LLY 3-column "ST %" group, cloned for "FP ST %". `HistoryAggregateViewsNestedAttributeMacro.viewdefn` (a separate KPI summary tile) also shows `sell_thru_pct`/`ly_sell_thru_pct`/variance — picks up the corrected value automatically, left without FP ST% added (not requested, mirrors CatRecap's Key Financials treatment). **Reworked to two-pass 2026-08-24**: this pivot's base tables were carrying an incomplete single-pass attempt from an earlier session (not yet QA-tested) — same HOCON/scope-var bug class as the rest of the family, plus two independent bugs found during the rework: (1) `BASE_SUM_OO_METRICS` was entirely missing the 10 new columns (87 vs the other 5 tables' 97, a union column-count mismatch); (2) `BASE_SUM_SALES_METRICS`/`BASE_LAST_INV_METRICS` were computing the new `tot_`/`fp_` columns as real values while still sitting in a WHERE-filtered (or about-to-be-filtered) table — same the-columns-must-live-in-a-separate-unfiltered-table issue as Quick. Fix: restored `AND prodlife IN (${LEAF_PRODLIFE})` on all 6 WHERE clauses (TY/LY/LLY × SALES/LAST_INV); zero-filled the 10 new columns in all 6 original base tables (added the missing set to OO); reverted `BASE_METRICS_UNION`'s 87 `sumIf(x, prodlife IN (${LEAF_PRODLIFE}))` back to bare `sum(x)` — the actual scope-var bug; added a single shared `BASE_SUM_SALES_METRICS_AGNOSTIC` param (87 zero-fill + 8 real tot_/fp_ columns) and 3 new unfiltered `TY/LY/LLY_BASE_SUM_SALES_METRICS_AGNOSTIC` tables (7th `UNION ALL` branch in `TY_ALL_BASE`/`LY_ALL_BASE`/`LLY_ALL_BASE`, matching CatRecap Detail's 6-table-union cardinality), `tot_last_value_eoh_u_x`/`_intransit_u_x` computed inline per year (scope-var subquery, kept out of any params.X block). Verified: all 6 original base tables 97 columns each, AGNOSTIC 95 params + 2 inline = 97; AGNOSTIC vs SALES column *order* diffed clean (the exact bug class caught in Aggregate Views Quick's own `BASE_SUM_SALES_METRICS`); zero scope-var refs inside any params.X block; zero comment-before-`params.X` adjacency; `PRE_BASE_METRICS_UNION`/`COMPUTED_METRICS`/`TY_COMBO_ALL`/`LY_COMBO_ALL`/`LLY_COMBO_ALL`/`FINAL_ALL_WITHOUT_VAR` confirmed untouched (already scope-var-free). | **Deployed & verified in QA** — 2026-08-24 |

### History Grid Tabs (all)

| Files | Pivot | Status |
|---|---|---|
| `HistoryHistoryGridNestedViewGrid.viewdefn`, `HistoryHistoryGridNestedViewGrid_quick.viewdefn`, `HistoryHistoryGridListViewGrid.viewdefn` | `HistoryGrid_Companion` / `HistoryGrid_quick` / `HistoryGrid_detail` — **these share `history_metrics_ty.ftl`** (the base-table fix already applies, confirmed via `history_quick.txt` runtime log: raw `tot_/fp_` columns compute correctly, zero unresolved `${...}`, no HOCON bug here since it's `<#include>`'d), but each has its own `COMPUTED_METRICS_PRUNED` copy that needed the same formula additions as HistoryFit_quick/_detail. **DONE — 2026-08-24**: added the 3-layer pass-through (`BASE_METRICS_UNION_PRUNED` → `BASE_METRICS_AGG_UNION_PRUNED` → `COMPUTED_METRICS_PRUNED`) for the 10 new raw columns, plus `tot_shp_u`/`tot_ret_u`/`tot_net_sls_u`/`fp_shp_u`/`fp_ret_u`/`fp_net_sls_u`/`tot_avail_inv_u`/`fp_st_pct`/`fp_woh`/`woh` and the corrected `sell_thru_pct` formula (old kept commented as `sell_thru_pct_OLD`) in `HistoryGrid_quick`/`HistoryGrid_detail` (full set). **`HistoryGrid_Companion` scoped down** to `sell_thru_pct` only (`tot_shp_u`/`tot_ret_u`/`tot_net_sls_u`/`tot_avail_inv_u`, no `fp_` columns) — checked its wrench (`uidefn/view/HistoryHistoryGridListViewGrid.viewdefn:182`, in the edit clone) and confirmed `fp_st_pct`/`woh`/`fp_woh` aren't wired to any column there, only `sell_thru_pct` (a real "Sell Thru %" grid column) and its band-color derivative — Companion is the parent row of a nested grid (department-level, with Quick's stylecolor rows nested underneath), so its `sell_thru_pct` still needed the fix to stay consistent with its own child rows. Verified: triple-quotes balanced, zero comment/`params.X` adjacency issues in all 3 files. **Wrench columns added 2026-08-24** (user explicitly asked for FP ST%/WOH/FP WOH in both Grid tabs' wrench, not just computed): added "FP ST %"/"WOH"/"FP WOH" grouped-column blocks (dataIndex `fp_st_pct`/`woh`/`fp_woh`) to both `HistoryHistoryGridNestedViewGrid_quick.viewdefn` and `HistoryHistoryGridNestedViewGrid.viewdefn` (Detail), right after the existing "ST %" column. **Correction same day**: initially set `visible: true` (forced always-on, copying `HistoryAggregateViewsNestedAttributeGrid_quick.viewdefn`'s pattern) — user clarified new metrics belong in the wrench's column chooser, not forced visible; switched all 3 groups in both files to `visible: false`, matching this file's own existing convention for optional columns (e.g. "APS R" is `visible: false` here too) — available to add via the grid's column chooser, not shown by default. Also found and fixed a THIRD copy of the stale `sell_thru_pct` formula in the shared rollup `HistoryStyleColorReviewHistoryGridViewRollUp.viewdefn` (a client-side `"formula"` field, not server-computed — referenced by both Quick and Detail tabs) — corrected to the agnostic `tot_net_sls_u`/`tot_avail_inv_u` version, matching the already-validated `HistoryStyleColorReviewRollUp.viewdefn` pattern; deliberately did NOT add `fp_st_pct`/`woh`/`fp_woh` to this rollup, mirroring that same file's established precedent (rollup kept lean, ST% only — no rollup changes needed beyond the formula fix). `HistoryGrid_Companion`'s wrench (`HistoryHistoryGridListViewGrid.viewdefn`) intentionally left without new columns, per the earlier scoping decision (only `sell_thru_pct` is shown there). All JSON validated. **`tot_weekcount` fix 2026-08-25**: same bug, same fix pattern — `HistoryGrid_quick`/`HistoryGrid_detail` each got `tot_weekcount` passed through `BASE_METRICS_UNION_PRUNED`/`AGG_UNION_PRUNED` (grouped at the end of their existing SUP-4378 sections) and swapped into the `woh`/`fp_woh` formulas (4 touch points each, confirmed via grep). `HistoryGrid_Companion` skipped entirely — confirmed 0 `woh`/`fp_woh` occurrences there (never added, since its wrench doesn't display them). `HistoryHistoryGridNestedViewGrid_quick.viewdefn`/`.viewdefn` confirmed to have zero `"formula"` fields anywhere (simple dataIndex bindings for the FP ST%/WOH/FP WOH columns added earlier) — no viewdefn changes needed, they inherit the fix automatically. Note: these 3 pivot files use CRLF line endings (unlike most others touched this ticket) — edits preserved the existing convention correctly (verified: new lines match, no mixed endings introduced). Verified: 34 triple-quotes balanced, 0 adjacency issues, UNION_PRUNED/AGG_UNION_PRUNED counts match in both files. | **DONE — awaiting QA validation, incl. `tot_weekcount` fix** |

## Assortment Build and Analysis (any view with current S/T% — add FP ST% + update ST%; Bottom Up + Top Down where it exists)

| Tab | Files | Pivot | Status |
|---|---|---|---|
| Summary View (all tabs) | `AssortmentBuildStyleColorSummaryDetails.viewdefn`, `AssortmentBuildStyleColorSummaryDetails_quick.viewdefn`, `AssortmentBuildStyleColorSummaryViewDetails.viewdefn` | verify — different pivot family (AssortmentFit*, not HistoryFit); base-table fix does **not** cover this, needs its own `history_metrics_ty.ftl`-equivalent check | Not started |
| Grid View | `AssortmentBuildStyleColorGridViewGrid.viewdefn`, `AssortmentBuildStyleColorGridViewGrid_quick.viewdefn` | verify | Not started |
| Flow Type | `AssortmentBuildStyleColorFlowTypeDetails.viewdefn`, `AssortmentBuildStyleColorFlowTypeSortBy.viewdefn`, `AssortmentBuildStyleColorFlowTypeViewRollUp.viewdefn`, `AssortmentBuildStyleFlowType*.viewdefn` | verify | Not started |
| Top TY vs LY | `AssortmentBuildStyleColorTopTYLYDetails.viewdefn` | verify | Not started |
| TY/LY Grid View | `AssortmentBuildStyleColorTYLYGrid.viewdefn` + GroupBy/List/ListSort/Rollup variants | verify | Not started |
| Cat Summary (all tabs) ⭐ ST only | `AssortmentAnalysisCatRecapCatSummaryGrid.viewdefn`, `AssortmentAnalysisCatRecapCatSummaryGrid_quick.viewdefn` | verify | Not started |
| Nested Attr (all tabs) ⭐ ST only | `AssortmentAnalysisNestedAttributeGrid.viewdefn`, `AssortmentAnalysisNestedAttributeGrid_quick.viewdefn` | verify | Not started |
| Nested View (+ FP ST% Rating, confirm ST% rating uses new logic) ⭐ ST only | not yet located — search for `*Rating*`/nested-view banding config | Not started |
| Nested Over Time ⭐ ST only (ticket notes an FP ST% metric **already exists here** — confirm its logic matches the new agnostic definition) | `AssortmentBuildNestedOvertime.viewdefn`, `TD_AssortmentBuildNestedOvertime.viewdefn` | **Check existing formula first — may need correcting, not adding** | Not started |
| Style Color over time | not yet located | Not started |

## Open items before continuing the rollout

1. **Confirm pivot family per row above** — several groups (Cat Recap, Aggregate Views, Assortment Build/Analysis) are NOT backed by `HistoryFit*`/`HistoryGrid*` and do not automatically inherit the `history_metrics_ty.ftl` base-table fix. Each needs its own base-table check before the formula/wrench edits.
2. **`Nested Over Time` already has an FP ST%** per the ticket text — this row needs a diagnose-first pass (is its current formula price-status agnostic already, or does it have the same bug we just fixed?) rather than a blind add.
3. **RP Weeks / FP Weeks definition** — hold metrics 5-7 across all views until Nicole confirms the reading, and until the `dbt_wk` vs `act_dbt_wk` divergence check + Jatin's first-sale-week/latest-MD-week approach call both land.
4. **ST-only rows (⭐)** need only the base-table agnostic fix + FP ST%/updated-ST% wrench entries — they do NOT need metrics 5-7 at all, so they're unblocked by item 3 above and can proceed independently once their pivot family is confirmed.
5. Once a few more view families are confirmed, this table should probably become the retro note's view-by-view record (`ticket-retro` skill).
6. **WOH/FP WOH group-recompute — `weekcount` aggregation choice unconfirmed.** Currently using `avg(weekcount)` in the wrench formulas (Grid View, TY/LY Grid View). Traced the ETL chain (`etl-belk-batch/ch_processing/insert_phistory_loc_arr.sql`) and found no week-spine/calendar join — `weekcount = count(distinct time)` is populated per style-color from whatever fact rows exist, so items with partial-scope tenure (mid-scope launches, early exits) show fewer weeks than full-tenure items. That means `avg(weekcount)` likely understates a grouped row's true weekcount (pulled down by partial-tenure items) and **understates WOH** as a result — `max(weekcount)` reasoned to be closer to correct, but not empirically confirmed (no BELK ClickHouse access via the db-readonly MCP right now; only TRD is registered there). **Hold this decision** until Hindsighting views are fully done, then cross-verify `sum`/`avg`/`max` of `weekcount` against real DB data (grouped multi-style-color scope with mixed item tenure) before finalizing which aggregation the wrench formulas should use.
