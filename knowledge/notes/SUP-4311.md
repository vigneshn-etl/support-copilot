---
ticket: SUP-4311
title: New text column cc_orig_unit_retail_char for Configure/GroupBy support
client: TRD
type: enhancement
repos: [etl, db]
components: [trd_ma_stylecolorattributes, trd_stylecolor_hier_attr, 010_products.sql, vertica_weekly_transform.sql, update_ticket_price, add_to_assortment, after_add_to_assortment]
symptom: "Frontend Configure/GroupBy (Cap Recap) cannot use Float/Real columns; need text variant '$'||cc_orig_unit_retail. Enables SUP-4152."
resolved: null
draft: true
status: IN PROGRESS — stage 5 (plan gate) passed; fix not started
---

## Plan (verified against code + QA schema dump 2026-07-09)

Add `cc_orig_unit_retail_char` (text, value `'$' || cc_orig_unit_retail`),
**appended LAST everywhere** (loads are ordinal — see gotchas):

1. Vertica `vsql/weekly/010_products.sql` ~line 545 CTAS — add the char
   expression next to `cc_orig_unit_retail`.
2. Vertica `trd_ma_stylecolorattributes_existing` DDL — add column
   (merge target AND the PG-export source; ticket missed this).
3. `vsql/daily/vertica_weekly_transform.sql` MERGE — THREE edits:
   UPDATE SET list (~314), insert(...) list (358), values(...) list (359).
4. PG `trd_ma_stylecolorattributes` — ALTER TABLE ADD COLUMN (last).
5. PG view `trd_stylecolor_hier_attr` — recreate with column appended
   (dump line 22075; explicit column list).
6. PG trigger fn `update_ticket_price` (dump 10860) — set
   `NEW.cc_orig_unit_retail_char` when retail changes.
7. PG fn `add_to_assortment` (dump 994) AND `after_add_to_assortment`
   (dump 3213) — BOTH insert into stylecolorattributes with explicit
   column lists; ticket named only the first. Add column to both.
8. CH `trd_stylecolor_hier_attr_tbl` + wrapper `trd_stylecolor_hier_attr`
   — database_changes/ Liquibase migration, column appended last.
9. NO script changes needed for daily/intraday/weekly exports —
   `export_to_ch.sql` uses `select * from` the view (all 3 flows covered).

## Gotchas (why ordering matters)

- Vertica→PG `\copy` csv load and PG→CH load
  (`input_format_with_names_use_header=0` = ORDINAL, header ignored) map
  by position. Column not appended last in ANY of the 6 objects ⇒ silent
  column shift. Verify with a header/count check on both hand-off files.
- No other writer touches cc_orig_unit_retail (spec-attr flow checked) —
  the 4 write paths above are complete.

## Open questions (ask reporter before fixing)

1. `'$' || numeric(16,4)` renders `$12.5000` — trailing zeros OK in
   group-by labels, or format to `$12.50` (TO_CHAR)? Decides the exact
   expression in all 4 write paths.
2. NULL retail → NULL char (blank group) or literal `$0`?
3. Does SUP-4152 need `cc_orig_unit_retail_cad_char` too? One migration
   pass is cheaper than two.

## Validation plan (execute at stage 7 — capture PRE state first)

- PRE: `count(*)`, `count(distinct cc_orig_unit_retail)` in PG + CH.
- POST: assert `cc_orig_unit_retail_char` matches formatted retail for
  100% of non-null rows in PG table, PG view, both CH tables.
- Trigger path: edit ticket price for one CC in QA UI → char updates.
- Function path: add-to-assortment one CC → char populated (both fns).
- Full weekly + one intraday run; column-count check on
  trd_ma_stylecolorattributes.csv and trd_stylecolor_hier_attr.tsv.
- Lineage regen + diff (expect no edge changes; column-level only).

## JIRA state

Draft stage-5 comment (plan + gaps + questions) prepared but NOT posted
yet. Labels to apply: cc-enhancement, cc-layer:etl, cc-layer:db,
cc-triaged. Base ticket: SUP-4152.
